To fulfill the requirements mentioned in the task, we need to perform the following steps:

Create Dockerfiles for the application and MongoDB.
Build Docker images for the application and MongoDB.
Create Kubernetes yaml manifests for the application and MongoDB (deployment and service).
Deploy MongoDB to the cluster using the created manifest.
Deploy the application to the cluster using the created manifest.
Verify the application is working correctly.
